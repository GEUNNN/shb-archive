import React, { FC } from "react";

const Header: FC = () => {
  return (
    <header className="flex justify-center items-center top-[10px] m-auto h-[55px]">
      <p className="text-center font-bold">햄냥이 아카이브</p>
    </header>
  );
};

export default Header;
