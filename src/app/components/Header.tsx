import React, { FC } from "react";

const Header: FC = () => {
  return (
    <header className="sticky top-[10px] m-auto">
      <p className="text-center font-bold">햄냥이 아카이브</p>
    </header>
  );
};

export default Header;
